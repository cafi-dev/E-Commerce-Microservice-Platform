version: 3
projects:
- dir: infrastructure
  workflow: default
  autoplan:
    enabled: true
    when_modified: ["*.tf", "*.tfvars"]
#- dir: appCD
#  workflow: default
#  autoplan:
#    enabled: true
#    when_modified: ["*.tf", "*.tfvars"]
